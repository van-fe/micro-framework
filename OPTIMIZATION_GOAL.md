# micro-framework 优化目标

状态：**已完成**。已于 2026-09-14 按 MFOPT-000 至 MFOPT-006 完成框架优化、回归与证据审计；已知 Runtime gzip 预算超限、真实 Safari 与长时 soak 未执行等交付边界保留在执行台账，不表示发布门禁全部通过。

执行台账：[OPTIMIZATION_LEDGER.md](OPTIMIZATION_LEDGER.md)。创建日期：2026-09-14。

## 目标

在保留独立 iframe Realm、Shadow DOM 和现有兼容语义的前提下，修复 micro-framework 卸载后的资源回收薄弱点，降低 HTML Entry 重复挂载及样式桥接开销，建立可复现的框架性能和内存回归证据。

本目标的实现位置是本仓库 `packages/`，验证位置是框架测试、示例与 `benchmarks/`。不以修改业务组件掩盖框架问题，不将业务源码、账号、内部配置或接口响应复制进框架仓库。外部业务项目只作为后续集成验证，不作为内核性能归因的唯一依据。

## 起点与证据边界

- 已审查框架版本：`aa6f5451bdfff1161445e54aad8c6b8e2456830c`，版本号 `0.0.1`。这是观察起点；执行者仍须检查新对话开始时的 HEAD、工作树和依赖变化。
- 外部业务评测三组中位数：重复切换第 1 → 10 轮，重构版 JS 堆约 `87.0 → 211.3 MiB`，Document 数 `17 → 44`；原版同期约 `91.5 → 110.9 MiB`，Document 数 `13 → 13`。
- 两个子应用菜单操作的中位耗时约 `0.580 → 1.463 s`、`1.256 → 1.973 s`。这些数字包含业务导航、网络和渲染，不是框架 mount 阶段计时。
- 外部性能样本来自不同构建器，且 Chromium 采样时尚未加入运行时 Sentry 拦截；只能作为问题线索。必须重新建立双方均禁止遥测、同构建器与同负载的优化前后基线。
- 源码可见薄弱点：宿主观察器/媒体查询直接暴露给应用，缺少统一回收登记；若干清理链在异常时会跳过后续清理；入口重复解析与样式完整刷新存在优化空间。**尚未确认真实泄漏的引用保留链，也未确认各候选热点的实际占比。**

## 不可改变的约束

1. 先读 `AGENTS.md` 和工程 Skill；修复落在最低责任层，保持包依赖方向。
2. JavaScript 仍在真实、独立的 iframe Realm 执行，DOM/CSS 仍在应用 ShadowRoot。不能用宿主全局补丁、伪 window、eval、运行时源码改写或跨 Realm 共享模块实例换取指标。
3. 保持原生观察器、事件、CSSOM、同步样式读取及脚本执行顺序；异步下载和执行顺序必须区分。
4. 缓存必须有容量和失效策略，隔离凭据及资源版本；不缓存 Window、Document、应用 DOM 或业务实例。保活/预热扩大须在回收问题解决后评估，不能以无限保留实例掩盖挂载成本。
5. **本地调试、预览、验收、基准禁止向 Sentry 上报，包括主应用和远程子应用。** `NEED_SENTRY=false` 只控制构建上传。浏览器必须在创建页面前安装网络拦截、禁用 Service Worker，并先以本地假接收端验证 fetch、XHR、Beacon、iframe、Worker 零外发。页面资源路由不得绕过拦截；本地指标仅保存本地文件。
6. 不降低已有门槛、不删除失败样本、不把候选原因记为已修复。工作树中的其他改动须保留。遵守工程 Skill 的本地提交规则；没有新授权不推送或发布。

## 完成标准

以下均为待验证目标，不代表已实现。

### 资源回收与异常清理

- 为媒体查询监听、ResizeObserver、IntersectionObserver 等宿主资源补充可证明的实例归属；覆盖显式移除/断开、重复调用、销毁后回调和跨应用互不影响。
- 清理函数故意抛错时，后续清理全部执行，实例引用仍置空；汇总保留错误，重复销毁安全。
- 以无业务数据的 HTML Entry 和组件库负载复现增长，记录引用保留链或可追踪的宿主资源持有者；补丁前用例失败、补丁后通过。若未复现，继续记录调查结果，不宣布真实业务泄漏已定位。
- 保留现有 Chromium 500 次销毁及 `24 MiB` 堆增长门槛。增加真实组件负载：每组预热 10 轮后，再执行 100 轮 mount/dispose，至少 3 个独立会话；每次采样先等待固定 5 秒，再显式 GC。同样负载的最终堆较预热后增长每组不超过 `24 MiB`，应用所属宿主监听/观察器/调度任务归零，存活 Document 不随轮次持续线性增长。阈值与样本配置在修改实现前固定。
- 保留完整采样序列、DOM/Document 数和堆指标；RSS 单独报告，不将其当作精确物理内存，不依赖 FinalizationRegistry 证明回收。

### 加载和样式性能

- 固定本地生产构建、浏览器版本、机器、测试负载、缓存/预热/保活策略和遥测设置；前后版本串行交错测试，至少 3 组、每组每场景 30 个计时样本，保存分组原始值、P50/P95。
- 新增 HTML Entry 组件负载的重复挂载基准：目标为三组 P95 中位数较冻结基线降低至少 `20%`；同负载首次挂载 P95 中位数不回退超过 `5%`。这是本轮优化目标，未达到须继续调查或明确记录未达成，不按结果事后放宽。
- 现有 ESM 冷挂载 P95 `1000 ms`、保活路由往返 P95 `10 ms` 门槛保持；不同场景不得混用指标。
- 分段测量入口下载/解析、Realm 初始化、桥接安装、脚本加载/执行、样式处理、生命周期及销毁。已有 DevTools 生命周期指标应扩展复用，默认关闭且仅本地导出。
- 样式优化必须证明减少重复扫描或布局读取，并通过动态样式、CSSOM、SVG、字体/rem、嵌套 ShadowRoot、弹层和同步读取回归。

### 兼容性与最终交付

- Chromium、Firefox、WebKit 真实浏览器覆盖适用的 React + Ant Design、Vue 3 + Element Plus、Vue 2 + Element UI、Vanilla 场景，以及 HTML/ESM 入口、取消、超时、失败恢复和快速切换。
- 最终执行工程 Skill 要求的冻结安装、架构检查、类型检查、完整构建、单测、Browser Mode、E2E，并运行 `bun run benchmark`。性能测试不与构建或其他浏览器测试同时运行。真实 Safari 和长时 soak 的执行状态单独记录。
- 若存在历史失败，记录基线复现、与本次变更关系及结果；新增失败必须解决，不能用历史问题笼统豁免。
- 交付代码、针对性回归、原始指标、前后对比、剩余限制，以及台账逐项证据、`UPDATES.md` 和本地提交。所有必做项均有验收证据后才完成目标。
- 已知 Runtime gzip 超出既定预算仍须在交付中保留披露；本目标不擅自扩大为打包体系重构，也不表示发布门禁全部通过。

## 新对话启动方式

在 `/Users/fanfangbo/workspace/micro-framework` 打开新对话，发送：

> 请启动 OPTIMIZATION_GOAL.md 中的 micro-framework 优化目标，按 OPTIMIZATION_LEDGER.md 执行并持续更新证据。从 MFOPT-000 固定基线开始，再处理资源回收和异常清理，随后优化加载和样式。只修改框架及其测试，不用业务补丁掩盖问题。所有本地浏览器测试必须先验证并启用 Sentry 零外发拦截。按仓库规则验证、更新 UPDATES.md 并本地提交，未经授权不推送。

执行者启动后先更新台账的执行分支、基线 SHA、当前项和下一步。需要新分支时使用 `feature/runtime-memory-performance`；如已存在，先检查其归属与状态，不覆盖已有工作。
