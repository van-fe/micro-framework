The first frozen full gate run exposed Worker cross-origin regressions after changing document.baseURI. Playwright was interrupted with SIGINT (PID observed as 57965; exit 130) after preserving failures, before modifying source. Unexecuted tests are not passes.

Files beginning with final- were moved here from the parent evidence directory. Their recorded argv and environment retain the original paths; resolve their archived logs and reports relative to this directory. The later successful final run uses the parent directory. Artifacts are copied under artifacts/.
